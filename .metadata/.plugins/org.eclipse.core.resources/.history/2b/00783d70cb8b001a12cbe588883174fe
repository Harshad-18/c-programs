#include <stdio.h>


int main() {

	int x,y,sum=0;
	int *p;
	int *q;

	p=&x;
	q=&y;

	printf("enter the first value: ");
	scanf("%d", &x);

	printf("enter the second value: ");
	scanf("%d", &y);

	sum=sum+ *p + *q;

	printf("Sum:%d",sum);

	return 0;
}
