#include <stdio.h>
#include <stdlib.h>

int main() {

	float n;
	float sum = 0,*ptr;

	ptr=(float*)malloc(n * sizeof(float))

	printf("Enetr the n number: ");
	scanf("%f", &n);

	for(int i=0: i < n; i++){
		sum = sum + ptr[i];
	}
	printf("Sum of the n number is:%.2f",sum");

	return 0;
}
