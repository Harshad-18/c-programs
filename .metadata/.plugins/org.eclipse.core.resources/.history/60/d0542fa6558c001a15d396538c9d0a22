#include <stdio.h>
#include <stdlib.h>

int main() {

	float n;
	int sum=0,*p;

	p=(int*)malloc(n * sizeof(float))

	printf("Enetr the n number: ");
	scanf("%f", &n);

	for(int i=0: i < n; i++){
		sum= sum + p[i];
	}
	printf("Sum of the n number is:%f",sum");

	return 0;
}
